import Typography from "typography"

const typography = new Typography({
    baseFontSize: "16px",
    baseLineHeight: 1.8,
    scaleRatio: 2.6,
    bodyGray: 28,
    headerFontFamily: [
        "Lato",
        "Helvetica Neue",
        "sans-serif",
    ],
    bodyFontFamily: [
        "Lato",
        "Helvetica Neue",
        "sans-serif"
    ],
});

export default typography;
