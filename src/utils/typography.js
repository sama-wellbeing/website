import Typography from "typography"

const typography = new Typography({
    baseFontSize: "16px",
    baseLineHeight: 1.8,
    scaleRatio: 2.6,
    bodyGray: 10,
    headerFontFamily: [
        "Roboto",
        "Helvetica Neue",
        "sans-serif",
    ],
    bodyFontFamily: [
        "Open Sans",
        "Helvetica Neue",
        "sans-serif"
    ],
});

export default typography;
